// test('Hola mundo en Jest', () => { })
// it('Hola mundo en Jest', () => { }) // Alias de test

// it y test son lo mismo, pero se recomienda usar test

describe("Grupo de pruebas", () => {
    test("Hola mundo en Jest", () => { });
    test("Otra prueba", () => { });
});

// Esta es una forma de crear test pero fuera de la carpeta __test__
